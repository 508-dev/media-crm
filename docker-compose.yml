services:
  media-crm:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - media-data:/data
    environment:
      - PORT=3000
      - DB_PATH=/data/media.db
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  media-data:
